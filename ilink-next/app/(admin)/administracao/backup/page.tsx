import { redirect } from 'next/navigation'; import Link from 'next/link'; import { getSession } from '@/lib/session';
export default async function BackupPage() {
  const session = await getSession();
  if (!session || session.nivel !== 1) redirect('/login');
  return (<div className="row"><div className="col-md-12"><div className="box box-white"><div className="box-header"><h3 className="box-title">Backup do Sistema</h3></div><div className="box-body"><div className="row"><div className="col-md-6"><h4>Criar Backup</h4><p className="text-muted">Faça o download de um backup completo do banco de dados.</p><form action="/api/admin/backup/criar" method="POST"><button type="submit" className="btn btn-primary btn-lg"><i className="lni lni-database"></i> Gerar Backup</button></form></div><div className="col-md-6"><h4>Restaurar Backup</h4><p className="text-muted">Restaure o sistema a partir de um arquivo de backup.</p><form action="/api/admin/backup/restaurar" method="POST" encType="multipart/form-data"><div className="form-group"><input type="file" name="arquivo" className="form-control" accept=".sql,.zip" /></div><button type="submit" className="btn btn-warning btn-lg"><i className="lni lni-reload"></i> Restaurar</button></form></div></div><hr /><h4>Backups Automáticos</h4><div className="table-responsive"><table className="table table-striped"><thead><tr><th>Data</th><th>Tamanho</th><th>Ações</th></tr></thead><tbody><tr><td colSpan={3} className="text-center">Nenhum backup automático disponível</td></tr></tbody></table></div></div></div></div></div>);
}
